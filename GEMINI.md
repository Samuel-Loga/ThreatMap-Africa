Threat Feed Page Updates

Display the severity tag on each feed entry
Show full country names (not codes) on threat feed entries
Add a search bar to search across indicator, description, and other relevant fields
Display the description provided by the submitter on each feed entry
Display the category in Title Case on each feed entry
The enriched tag should be in line with severity and TLP tags
Show submitted description for each feed
On filters, add filter by severity (Critical, High, Medium, Low, Info) - title case
On country filter, use full country names in dropdown menu - title case
Use title case for all drop down filter values
Use title case for all tags used on all given feeds
Add search input to the right of "Threat Feed" title on threat feeds page to allow seach by indicator, country, etc